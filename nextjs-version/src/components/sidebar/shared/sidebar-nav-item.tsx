import { NavMain } from "@/components/nav-main"

/**
 * Shared UI button styles for sidebar navigation items.
 * Currently delegating to our standard NavMain to maintain consistency across the app.
 */
export const SidebarNavItem = NavMain
